import { TitleTransfPipe } from './title-transf.pipe';

describe('TitleTransfPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleTransfPipe();
    expect(pipe).toBeTruthy();
  });
});
